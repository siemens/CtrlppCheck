// start options:
// error id: 
// support for WinCC OA datatype vector 
#uses "std"

void main()
{
  int i = 0;
  vector <string> v = 2;
  v.at(0) == NULL;
  i = getRand();
  shared_ptr<int> ip  = 10;

  if ( i == 0 )
   v = nullptr;
}