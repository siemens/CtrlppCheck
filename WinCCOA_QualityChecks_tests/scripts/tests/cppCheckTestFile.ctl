// http://cppcheck.sourceforge.net/manual.html#idm739
void main()
{
  int ii;
  int i = 1;
  string str;
  for (i ; i<j ; i++)
  {
    int d = i +1;
    DebugTN(d);
  }
  if ( ii == 0 )
    foo("aufruf");
	
  foo2();
  foo3();
  
  anytype at = 2;
//   someType sat = 2;
//   someType sat2;
  dyn_atime dat;
  
  
  float i2 = foo2(null);
  atime i3 = foo3(null);
}

void f(int x)
{
    if (calculate(x) == 213) {
    
    } else if (calculate(x) == 213) {
         // unreachable code
    }
}

int foo (string s)
{
  isfile(s);
  bool b = isfile();
  int i = isfile("abc");
  return 0;
  DebugTN("foo");
}

int foo2(string str = getPath())
{
  if ( isfile(str) )
    return -3;
  else if ( isfile(str) )
    return -4;
	

}

int foo3()
{
  int i = 2;
  dyn_mixed dm;
  return;
}

